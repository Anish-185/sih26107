# Runtime dependencies for the MetrIQ / BIS Assistant backend.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0

# HTTP client used by app/llm.py to talk to the local LM Studio server
# (OpenAI-compatible /v1/chat/completions). Required for the grounded
# explanation endpoints (/ask, /certification-guidance, /laboratory-search);
# without it `import app.main` fails.
httpx>=0.27,<1.0
