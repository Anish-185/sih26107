# Runtime dependencies for the MetrIQ / BIS Assistant backend.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0

# HTTP client used by app/llm.py to talk to the local LM Studio server
# (OpenAI-compatible /v1/chat/completions). Required for the grounded
# explanation endpoints (/ask, /certification-guidance, /laboratory-search);
# without it `import app.main` fails.
httpx>=0.27,<1.0

# Inspection pipeline: local image decode + OCR for POST /inspection/analyze.
#  - python-multipart  : FastAPI multipart/form-data (file upload) support
#  - pillow, numpy     : image decode, EXIF orientation, quality metrics
#  - rapidocr-onnxruntime : PaddleOCR PP-OCRv3 model weights run via ONNX Runtime
#    (PaddlePaddle has no wheels for this Python; the models are identical and
#    ship inside the wheel, so OCR is fully local with no network at inference).
python-multipart>=0.0.9
pillow>=10,<13
numpy>=1.26
rapidocr-onnxruntime>=1.2,<1.3
