# Copy this file to `.env` and fill in real values as later phases need them.
# Nothing here is required for Phase 1.

# --- Database (Phase 2) ---
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/bis_assistant

# --- LLM provider (Phase 4) — provider must stay replaceable ---
# LLM_PROVIDER=
# LLM_API_KEY=
# LLM_MODEL=
